# Bundleen

This repository contains the Bundleen responsive Next.js web application.

## Web application

Application source lives in `src/`, with static assets in `public/`.

```bash
npm install
npm run dev
```

Open <http://localhost:3000>.
