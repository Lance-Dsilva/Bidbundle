/**
 * Test stub for the `server-only` package.
 *
 * The real module throws when imported outside a server context, which is
 * exactly what it is for. Unit tests import the guarded modules directly, so
 * the alias in `vitest.config.ts` points here instead.
 */
export {};
